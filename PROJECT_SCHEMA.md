# 📁 MondMaker Project Data Schema Specification

เอกสารระบุมาตรฐานข้อมูลของแต่ละโปรเจกต์ (Portfolio) สำหรับใส่ในไฟล์ `src/data/projects_data.json` เพื่อให้สามารถนำมาแสดงผลบนเว็บไซต์ได้อย่างสมบูรณ์แบบ ทั้งข้อความ รูปภาพ วิดีโอ หรือตัวอย่าง 3D

---

## 📄 โครงสร้างข้อมูล JSON (Schema)

```json
{
  "id": "slug-name-only",
  "title": "ชื่อโปรเจกต์อย่างเป็นทางการ",
  "category": "smart-iot | web-automation | 3d-printing",
  "tags": ["IoT", "Web", "3D Printing"],
  "businessOutcome": "คำอธิบายผลลัพธ์เชิงธุรกิจ (ถ้าไม่มีสามารถละเว้นหรือใส่เป็นค่าว่างได้)",
  "summary": "สรุปการทำงานสั้นๆ 1-2 ประโยค",
  "media": {
    "thumbnail": "/images/projects/project_slug/thumb.jpg",
    "videoUrl": "https://www.youtube.com/watch?v=XXXXXX",
    "model3dUrl": "/3d-model/project_slug/model.glb",
    "gallery": []
  },
  "techSpecs": {
    "embedded": [],
    "web": [],
    "design3D": [],
    "automation": []
  },
  "features": []
}
```

---

## 🔍 ความจำเป็นของแต่ละฟิลด์ (Required vs Optional)

ไม่จำเป็นต้องกรอกทุกฟิลด์ครับ! ด้านล่างนี้คือการแบ่งกลุ่มฟิลด์ที่ **"ต้องมี (Required)"** และ **"จะมีหรือไม่ก็ได้ (Optional)"** เพื่อให้ยืดหยุ่นตามความพร้อมของข้อมูลแต่ละโปรเจกต์:

### 1. ฟิลด์ที่จำเป็นต้องมี (Required - ❌ ห้ามขาด)
*   **`id`**: ใช้ระบุตัวตนของโปรเจกต์ (เช่น `smart-cold-room`) เป็นภาษาอังกฤษตัวเล็ก คั่นด้วยขีดกลาง
*   **`title`**: ชื่อของโปรเจกต์ที่แสดงผลบนหน้าเว็บ
*   **`category`**: ต้องเป็น `smart-iot`, `web-automation`, หรือ `3d-printing` เท่านั้นเพื่อให้ระบบจัดกลุ่มถูก
*   **`tags`**: คีย์เวิร์ดสั้นๆ (อาเรย์ของ String) สำหรับใช้จัดประเภทแท็กบนหน้าการ์ด เช่น `["IoT", "Next.js"]`
*   **`summary`**: อธิบายสั้นๆ ให้คนอ่านรู้ว่ามันคืออะไรภายใน 1-2 บรรทัด
*   **`media.thumbnail`**: พาธรูปหน้าปกของโปรเจกต์ที่จะขึ้นโชว์บนการ์ดหน้าแรก (เช่น `"/images/projects/cold-room-thumb.jpg"`)

### 2. ฟิลด์ที่เลือกใส่ได้ (Optional - 💡 มีก็ดี ไม่มีก็เว้นว่างไว้ได้)
*   **`businessOutcome`**: ถ้าไม่มีความต้องการเน้นเชิงธุรกิจ สามารถปล่อยว่างเป็น `""` หรือไม่ใส่ได้ แต่ถ้ามีจะช่วยดึงดูดลูกค้าดีมาก
*   **`techSpecs`**: อาเรย์ย่อยข้างใน หากไม่มีตัวไหนให้ใส่เป็นอาเรย์ว่าง `[]` ได้เลย เช่น หากไม่มีส่วนเว็บก็ใส่ `"web": []`
*   **`features`**: อาเรย์ของความสามารถเด่น ถ้าไม่มีฟีเจอร์ย่อยให้ใส่เป็นอาเรย์ว่าง `[]`

---

## 🎬 อธิบายรายละเอียดในส่วนของ `media` (Media Block Breakdown)

ในส่วน `media` ถูกออกแบบมารองรับสื่อหลายรูปแบบเพื่อทำให้โปรเจกต์ดูน่าสนใจ:

1.  **`thumbnail` (ต้องมี - Required)**
    *   **หน้าที่**: รูปภาพหน้าปกสำหรับแสดงบนการ์ดโปรเจกต์ (Card List) บนหน้าหลัก
    *   **ขนาดที่แนะนำ**: 16:9 (เช่น 1280x720 หรือ 800x450 พิกเซล) เพื่อให้แสดงผลสัดส่วนสวยงามไม่บิดเบี้ยว
    *   **พาธไฟล์**: เก็บไว้ในโฟลเดอร์ `public` เช่น `"/images/projects/cold-room-thumb.jpg"`

2.  **`videoUrl` (เลือกใส่ - Optional)**
    *   **หน้าที่**: ลิงก์วิดีโอสาธิตการทำงานจริงของโปรเจกต์
    *   **รูปแบบ**: แนะนำให้ใช้ลิงก์ **YouTube** (เช่น `"https://www.youtube.com/watch?v=dQw4w9WgXcQ"`) หรือลิงก์วิดีโอที่ฝากไว้ภายนอก เพื่อไม่ให้เปลือง Bandwidth ของตัวเว็บ และช่วยให้เว็บโหลดเร็วขึ้น
    *   **การใช้งาน**: เหมาะมากสำหรับโปรเจกต์ประเภท Smart IoT (เช่น โชว์บอร์ดส่งการแจ้งเตือน) หรือ Web Automation (เช่น อัดหน้าจอการทำงานของบอท)

3.  **`model3dUrl` (เลือกใส่ - Optional)**
    *   **หน้าที่**: พาธของไฟล์โมเดล 3D นามสกุล `.glb` เพื่อเปิดให้ลูกค้ากดคลิกและหมุนดูชิ้นงานได้จากหน้าเว็บโดยตรง
    *   **ขนาดที่แนะนำ**: ไม่ควรเกิน **5MB** (แนะนำให้ย่อ/บีบอัดโพลีกอนก่อนเซฟ) เพื่อป้องกันไม่ให้หน้าเว็บค้างหรือโหลดช้า
    *   **การใช้งาน**: เหมาะมากสำหรับโปรเจกต์ 3D Design/3D Printing ที่เราออกแบบเคสผลิตภัณฑ์เอง

4.  **`gallery` (เลือกใส่ - Optional)**
    *   **หน้าที่**: ภาพประกอบเพิ่มเติม เช่น ภาพหน้าจอ Dashboard หลายๆ หน้า, ภาพถ่ายวงจรภายในบอร์ด หรือขั้นตอนการประกอบชิ้นงาน
    *   **รูปแบบ**: อาเรย์ของที่อยู่ไฟล์รูปภาพ เช่น `["/images/projects/pic1.jpg", "/images/projects/pic2.jpg"]`
    *   **หากไม่มีภาพเพิ่ม**: ให้ใส่เป็นอาเรย์ว่าง `[]`
